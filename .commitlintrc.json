{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "header-min-length": [2, "always", 1],
    "header-max-length": [2, "always", 72],
    "subject-case": [0],
    "scope-enum": [1, "always", ["cli", "react", "fluent-icons", "repo", "deps", "ci", "release"]]
  }
}
