# Generated by `lcui build` (compile) when the test runs.
# Only input fixture files (package.json, configs, app/*.tsx, app/*.css) should be tracked.

.lcui/
dist/
node_modules/

# Loader emits *.h / *.c next to each source file, plus main.c/main.h.
app/*.h
app/*.c
app/*.css.h
