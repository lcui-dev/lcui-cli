# Generated by `lcui build` (compile) when the test runs.
# Only the input fixture files (package.json, app/*.tsx) should be tracked.

.lcui/
dist/
node_modules/

# Loader emits *.h / *.c next to each source file, plus main.c/main.h.
app/*.h
app/*.c